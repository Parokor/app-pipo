import { UnderwaterOasis } from '../underwater_oasis.ts';

document.addEventListener('DOMContentLoaded', () => {
  const container = document.getElementById('underwater-oasis-container');
  new UnderwaterOasis(container);
});
